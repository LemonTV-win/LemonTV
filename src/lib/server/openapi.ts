// The OpenAPI spec is generated at build time by `scripts/generate-openapi.ts`
// (run as part of `build` / `openapi:generate`) and imported here as static
// JSON. We cannot run swagger-jsdoc at runtime because it scans the
// `src/routes/api/**` source files, which are not present in the deployed
// serverless bundle. Regenerate with `bun run openapi:generate`.
import swaggerSpecJson from './openapi.generated.json';

export const swaggerSpec = swaggerSpecJson;
